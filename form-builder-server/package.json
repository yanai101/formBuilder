{
  "name": "builder-server",
  "version": "1.0.0",
  "description": "Builder Server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --inspect server.js",
    "serve": "server.js",
    "sudo": "sudo ./node_modules/.bin/nodemon --inspect server.js"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.17.1",
    "mongodb": "^3.5.1",
    "mongoose": "^5.8.8",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
